# DESIGN.md — YamaGuessr のデザインの正

> このファイルは **後続の作業がデザイン判断を引き継ぐための引き継ぎ書**。
> UIを触る前に必ず読む。値の正は [src/styles/tokens.css](src/styles/tokens.css)。
> トークンに無い色・書体・余白をコンポーネント側でベタ書きしない。

## 題材（1文）

**登山者が、自分の山行の1コマ（写真または周辺の3D地形）だけを手がかりに、国土地理院の地形図上で「そのとき自分がどこにいたか」を指し当てるゲーム。** 使うのは開発者と友人・知人。どの画面も仕事はひとつ、「見る → 地図を指す → 答え合わせ」。

## 中心にある制約

**地図が主役で、UIは主役の邪魔をしない。** 地形図タイル・3D地形・フレーム画像が画面の情報のほぼ全部であり、UIはそれを読む妨げになってはいけない。したがってUIは「図郭の外側」——画面の上下の薄い帯——に退き、面積を主張しない。

## Color

地形図タイルは白地に茶の等高線で**明るい**。だからUIのクロームは**暗く沈める**。明暗を反転させることで、余計な枠線や影を足さずに地図そのものが浮き上がる。この配色は「地図が主役」という制約から導いたもので、雰囲気で選んだダークテーマではない。

| トークン | 値 | 用途と根拠 |
|---|---|---|
| `--c-ridge` | `#10161F` | 稜線の影。アプリ背景 |
| `--c-dusk` | `#1A2430` | 夜明け前の空。パネル面 |
| `--c-mist` | `#27333F` | ガス。境界線 |
| `--c-snow` | `#EDF1F5` | 残雪の白。前景テキスト。純白にせずわずかに青みを残す |
| `--c-slate` | `#93A1AF` | 補助テキスト。**Tailwindの無彩色グレーは使わない**。青みを含ませて画面が冷たく無個性になるのを防ぐ |
| `--c-alt` | `#E8A33D` | アクセント・主アクション。**色別標高図の「高さ」の色**から。このゲームの本質はピーク・コル・尾根＝高さなので、アクセントを標高の色にする |
| `--c-near` / `--c-far` | `#5FBF8A` / `#D9604D` | 結果の良し悪し。**色だけに意味を持たせず、距離(m)と点数のテキストを必ず併記する** |
| `--c-contour` | `#A9793F` | 地形図の等高線の茶。距離リング（下記シグネチャ）に使う |
| `--c-water` | `#4A90C4` | 地形図の水系の青。推測地点のマーカー |

**コントラスト（実測）**：`--c-snow` on `--c-ridge` ≈ 16:1、`--c-slate` on `--c-dusk` ≈ 6.0:1、`--c-alt` on `--c-ridge` ≈ 8.5:1、`--c-ridge` on `--c-alt`（主ボタンの文字）≈ 8.5:1。いずれもWCAG AA（4.5:1）を満たす。

## Type

**Webフォントを読み込まない。** 外部依存を増やさない規約（AGENTS.md）に従い、かつ地図タイルの読み込みと帯域を奪わないため。代わりに**2つのロールの使い分け**で個性を出す。

- `--font-num`（等幅）：**標高・距離・スコア・座標のすべての数値**に使う。地形図の標高注記や三角点の数値の質感であり、桁が揃うので「1,284m」と「284m」を読み違えない。**これがこのプロジェクトの書体上の署名。**
- `--font-body`：本文・UI文言。日本語のシステムフォントを優先。

「どの案件でも Inter」という初期値は踏んでいない。

## Layout & space

中心概念は**「図郭」**。地図が常に画面の最大面積を占め、UIは上下の薄い帯として乗る。カードを敷き詰めない。等間隔グリッドで情報の軽重を潰さない。

- 間隔：4px基準（`--s-1`〜`--s-12`）
- 角丸：`6px`/`8px` と控えめ。地図UIは丸すぎると玩具に見える
- 影：**地図の上に浮く要素だけ**。面の分離は1pxの線（`--border`）で行う

## Signature — 距離リング

**回答後、正解地点を中心に等高線と同じ細い茶の同心円（100m / 250m / 500m / 1km / 2km）を地形図に描き、自分の推測がどのリングに入ったかを見せる。**

GeoGuessr は直線と距離しか出さない。しかし山では**等高線こそが土地勘の言語**であり、「500mリングの中には入った」という理解のしかたが、この題材でだけ意味を持つ。大胆さはここに集中させ、周囲のUIは静かに保つ。

実装は `src/map/rings.ts`。リングのラベルも `--font-num` で描く。

## 避けると決めた初期値

以下は明示の指定がない限り使わない。

- **暖色クリーム型**（クリーム地＋セリフdisplay＋テラコッタ `#D97757` 系アクセント）。`--c-far` を赤系にする際も、テラコッタに寄らないよう彩度を上げて `#D9604D` にしている
- **黒地＋蛍光アクセント型**（純黒 `#000` は使わない。最暗部は `#10161F`）
- **新聞ブロードシート型**（ヘアライン罫線＋角丸ゼロの多段組）
- 紫グラデーション、過剰な glassmorphism
- Tailwind の無彩色グレー素通し
- 見出し・機能ラベルの絵文字
- 意味のない `01 / 02 / 03` 連番
- `Inter` 一択

## 不変条件（壊してはいけない制約）

これらは見た目の都合で曲げない。多くは利用規約・仕様上の要件。

1. **地理院タイルの出典表示（「地理院タイル」＋出典URLへのリンク）を、地図・3Dビューの両方で常時表示する。** 利用規約上の必須事項（docs/operations.md）
2. **3D地形に、地名・注記・等高線ラベルを含む地形図タイル（標準地図・淡色地図）をテクスチャとして貼らない**（一人称・3人称とも）。答えが画面に表示される（docs/spec.md 設計判断表）。テクスチャは注記の無い空中写真を使う
2b. **モード①の3Dは一人称視点を崩さない。** カメラは**地面＋目線の高さ（1.5m）**に置き、ドラッグでは視線だけを回す（**頭は動かさない。見回しで見下ろし角も変えない**——地形が何であれ操作した角度そのまま）。既定の視線は**水平**で、**見上げ・見下ろしは±40度**まで（それ以上は俯瞰の画になる）。移動はルートの上だけ（ストリートビュー式）で、**動いたことが分かるようにアニメーションで見せる**。「この地点／方位／視線の上下／標高／出発地点から○m」の表示と、離れたときに出る出題地点のピンも外さない（**出すのは現在の1点だけ。地形の裏に回ったら透かさず消す**）。**足元の地面を近クリップ面で切り取らない**（穴からメッシュの裏側が見える。`src/map/eye.ts` の `aimDistanceM`）
2b'. **モード②の3Dは3人称を崩さない。** 出題地点を中心に**外から回して眺める**。中心は動かさない（パンできない）。毎回**北が上・同じ角度**から始める。描くのは**出題地点のマーカーだけ**——ルートも写真も出さない（地形の形だけを読む問題にする）
2c. **地形図には歩いたルートを常に描く。** 手がかりが無いと当てずっぽうになる。線は白いハローを敷いた朱で、等高線に埋もれさせない。**一人称の3Dにも同じ朱で描く**——空中写真の地面の柄に紛れない太さを取る（細すぎて見失うほうが、太くて滑走路に見えるより悪い）。**3人称には描かない**（不変条件2b'）
2d. **出題画面は「見る」と「答える」を1画面ずつに分ける。** 3Dも地形図もそれ自体が読む対象で、半分ずつに割るとどちらも読めない。タブで切り替え、選んだ側が画面いっぱいを使う。主アクションは各面に1つ（見る＝「地形図で答える」／答える＝「回答する」）
3. **正解座標を画像ファイル名・EXIFに含めない**
4. **地図と3Dビューの上に、UI要素を必要以上に重ねない。** 地図が主役
5. **タップ領域は最低 44×44px**、入力欄のフォントは **16px以上**（iOSの自動ズーム回避）
6. **色だけで状態を表さない。** 正誤・距離の良し悪しは必ずテキストを併記する
7. **Supabaseに繋がらなくてもゲーム本体は最後まで動く**（docs/spec.md 機能K）

## 画面の一覧（このデザインの適用範囲）

| 画面 | 唯一の仕事 | 主アクション（1つ） |
|---|---|---|
| ホーム | 遊び方・手がかり・コースを選ばせる | 「はじめる」 |
| 出題（見る） | 3D地形（と写真）を読ませる | 「地形図で答える」 |
| 出題（答える） | 地図を指させる | 「回答する」 |
| 結果（1問） | どれだけ外したかを理解させる | 「次の問題」 |
| セッション結果 | 通しの成績を見せる | 「もう一度」 |
| リーダーボード | 順位を見せる | 「ホームへ」 |

`pipeline/review.html` は**開発者専用のローカルツール**で、この体系の適用範囲外（同系統の暗色は使うが、密度優先で独自に組んでいる）。

## 変更履歴

- 2026-07-29 初版。トークン体系・シグネチャ（距離リング）を確定。
- 2026-07-29 GPXトラックの常時表示、ホームのコース選択、3Dビューの一人称化（不変条件 2b / 2c を追加）。
- 2026-07-30 3Dビューをストリートビュー式に作り直し（目線1.5m・ルート上を移動・俯瞰なし・ルート全体を細線で描画）。不変条件 2b / 2c を更新。ホームにローカル専用のstudio導線を追加（localhostのときだけ）。
- 2026-07-30 3D地形のテクスチャに地理院の空中写真（`seamlessphoto`）を貼り、陰影は薄く重ねる形に変更（不変条件2を更新）。陰影だけでは目の前の斜面が単調な面に見えていたため。地形図（標準地図）は注記が写るので引き続き貼らない。
- 2026-07-30 3Dの一人称を詰める：見回しで見下ろし角を変えない（頭が動いて見えないように）、移動をアニメーションで見せる、出題地点にピンを立てる（不変条件2bを更新）。
- 2026-07-31 1問5分の持ち時間（タイマー）を追加。考えた時間と3Dで歩いた推定コースタイムを合わせて使い、**残り時間バーの残量＝点数の倍率**という対応にした（見たまま判断できる）。タイマーはタブの下＝どちらの面でも見える位置に固定。画角を52度に広げ、周りの尾根が視界に入るようにした。
- 2026-07-31 MapLibre を v5 に上げ、一人称カメラを「視線上の一点を中心に置く」形に作り直した（`centerClampedToGround: false`）。**見下ろし角が地形で揺れなくなり、水平・見上げも表せるようになった**（不変条件2bを更新）。
- 2026-07-31 出題画面を「見る／答える」のタブに分割（不変条件2dを追加）。モードを視点で分け直し、一人称3Dはモード①へ、モード②は3人称の地形モデルに（不変条件2b'を追加、2・2b・2cを更新）。一人称は近クリップ面が足元を切らない範囲までしか狙わない（メッシュの裏側対策）。3Dのルート線を太くした。
